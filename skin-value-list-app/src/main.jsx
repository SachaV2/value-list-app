import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import SkinValueListApp from './SkinValueListApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SkinValueListApp />
  </React.StrictMode>,
);
